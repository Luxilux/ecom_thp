
  <p>
<%= form_for @item do |f| %>
<div class="form-group">
<%= f.text_field :title, placeholder: "Cat_name", class: 'form-control' %>
</div>
<div class="form-group">
  <%= f.text_field :category_id, placeholder: "Catégorie", class: 'form-control' %>
</div>
<div class="form-group">
  <%= f.text_field :description, placeholder: "Description", class: 'form-control' %>
</div>
<div class="form-group">
  <%= f.number_field :price, placeholder: "Prix", class: 'form-control' %>
</div>

<div class="form-group">
  <button class="btn btn-primary" type="submit" > Ajouter ce produit </button>
</div>

<% end %>
